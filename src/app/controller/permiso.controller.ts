import { Request, Response } from "express";
import mysql from "../config/database";
import { Permiso } from "../model/permiso.model";

class PermisoController {
  constructor() {}
}

const permiso = new PermisoController();
export default permiso;
